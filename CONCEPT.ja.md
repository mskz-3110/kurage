# コンセプト

## 背景

`bash` は手軽に使える一方で、変数の扱いが直感的ではなく、途中でエラーが起きたときの制御やデバッグもしづらい。処理が少し複雑になるだけで、読みやすさや保守性が急に落ちやすい。

そのため、型と構造を持った形でコマンドや簡単なスクリプトを扱える実行環境がほしいと考えた。

## 目的

kurage は、`TypeScript` ベースでコマンド実行、対話的な試行、スクリプト実行をまとめて扱える小さな実行基盤を作ることを目的とする。

スクリプトを書かずにその場で試したいときは `REPL` を使い、単発の処理は `exec` で、ファイルにまとめた処理は `run` で実行できるようにする。

対話的な試行と `exec`、`run` をひとつの流れで扱える、柔軟な実行環境を目指す。

## package.json の description

`package.json` の `description` は、このコンセプトを短く要約したものとして扱う。

| English | 日本語 |
| --- | --- |
| A flexible runtime for REPL, exec, and run. | REPL、exec、run のための柔軟な実行環境。 |

## 対象ユーザー

- `bash` の変数や引数の扱いに不満がある人
- 途中で失敗したときの制御やデバッグをもっと扱いやすくしたい人
- 対話的に試しながら処理を組み立てたい人
- 保守しやすい形でコマンド処理を扱いたい人

## 提供したい体験

- `kurage` だけで `REPL` に入れる
- `kurage exec ...` で渡したコマンドをその場で実行できる
- `kurage run <file>` でファイルパス指定のスクリプトを実行できる
- スクリプトを書かずにすぐ動作確認できる
- 失敗時の挙動を追いやすく、デバッグしやすい

## 技術方針

- 実装言語は `TypeScript`
- 実行環境は `Node`、`Bun`、`Deno` を想定する
- 外部パッケージには依存しない
- 環境差分はできるだけ小さく保つ
- 標準機能を優先し、必要以上に複雑にしない
- 最初に実行されるファイルの読み込みは `dynamic import` を前提とする
  - `REPL` とスクリプト実行で同じファイルを共有しやすくするため
  - 実行の入口に応じて、読み込む対象を動的に切り替えられるようにするため
- `TypeScript` ベースで実装し、実行時は `JavaScript` として動作するようにする
  - 型と構造を持たせることで、実装時の読みやすさと保守しやすさを高めるため
  - `TypeScript` の表現力を使いながらも、実行時には広く使える `JavaScript` に落とし込むことで、`Node`、`Bun`、`Deno` で扱いやすくするため
  - 実装言語と実行形式を分けて考えることで、設計の自由度と実行時の互換性の両方を確保しやすくするため

```js
const {kurage} = await import('kurage');
```

## 入出力方針

- 標準入力、標準出力、標準エラーの扱いは、コマンドライン実行の基本に沿って整理する
- `exec` では標準入力をそのままコマンドへ中継できるようにする
- `run` ではファイルから読み込んだ処理を、実行結果とともに素直に扱えるようにする
- エラーは標準エラーに出し、通常の出力と分けて扱えるようにする
- 対話実行とスクリプト実行のどちらでも、入出力の見え方が大きく変わらないようにする

## エラー方針

- 処理途中で例外が発生した場合は、標準エラー出力に例外情報を表示して異常終了する
- `REPL` では、継続できる範囲では対話を続け、致命的な例外は同様に異常終了する

## 検証方針

- 各実行環境での動作確認には `Docker` を使う
- ローカル環境に依存しすぎない形で再現性を確保する

## 入口設計

- `kurage`
  - REPL
  - 対話的に動作確認するための入口
- `kurage exec <command>`
  - 渡したコマンドを実行する
  - 標準入力は指定したコマンドへ中継できる
- `kurage run <file>`
  - ファイルパスで指定したスクリプトを実行する

## 実行形態の役割

- `kurage`
  - 1行ずつ試しながら処理を組み立てるための対話モード
  - 小さな検証や、挙動を確認しながら進めたいときに使う
  - スクリプトとして書き出す前に、式や処理の組み合わせを試せるようにする

- `kurage exec <command>`
  - 1回だけコマンドを実行したいときの入口
  - その場で与えた処理を、対話なしで実行する
  - 短い確認や単発の処理を、スクリプト化せずに実行できるようにする
  - 標準入力を受け取れるようにし、既存のコマンドライン利用に近い感覚で扱えるようにする

- `kurage run <file>`
  - ファイルにまとめた処理を実行する入口
  - 複数行の処理や、再利用したいロジックを扱う
  - 実行対象がファイルとして明確なので、後から読み返しやすく、共有もしやすい
  - 単発実行ではなく、継続的に育てるスクリプトの入口として位置づける

- 共通方針
  - どの入口でも、同じ言語仕様と実行ルールで動くことを目指す
  - 入口ごとの差は、主に「入力の与え方」と「対話性」に限定する
  - 使い方が違っても、実行結果やエラーの見え方はできるだけ一貫させる

## 課題認識

`bash` には次のようなつらさがある。

- 変数の扱いが直感的ではない
- 例外処理がなく、失敗時の制御が書きにくい
- バックトレースが弱く、デバッグしづらい
- クォートや空白の扱いで事故が起きやすい
- 複雑な処理になるほど保守しづらい

kurage は、こうしたつらさを減らすための実行基盤として考える。

## 位置づけ

既存の `REPL` やシェル、ランタイムはあるが、kurage はそれらの単なる置き換えではなく、`bash` の扱いづらさを減らすことを目的にした、シンプルな実行インターフェースを目指す。

## 成功条件

- `REPL`、`exec`、`run` の3つの入口が一貫した実行体験として動作する
- `bash` よりも、変数や処理の流れを追いやすいと感じられる
- 失敗時に原因を追いやすく、デバッグしやすい
- `TypeScript` ベースで、読みやすく保守しやすい形を保てる
- `Node`、`Bun`、`Deno` の差をできるだけ意識せずに使える
- 外部パッケージに依存しない方針を保ちながら、実用的な実行環境として成立する

## 非対応事項

- `CommonJS`
  - `require()` や `module.exports` を前提とした実装や利用は対象外とする
  - `ES Modules` を前提に設計する
- `eval`
  - 文字列をそのまま評価して実行する形は対象外とする
  - 実行対象を `kurage run <file>` と `REPL` に絞り、何が実行されるかを明確に保つ
  - 文字列実行を増やさないことで、安全性とデバッグしやすさを優先する
- 完全な `bash` 互換
  - `bash` の全機能や細かな挙動まで再現することは対象外とする
  - `bash` の置き換えではなく、扱いやすい実行インターフェースを作ることを優先する
- 外部パッケージ前提の設計
  - `npm` などの外部パッケージに強く依存する構成は避ける
  - 標準機能を中心に、できるだけ小さく保つ

## 名前の由来

シェルの硬いイメージに対して、より柔軟で流動的な実行環境を目指したいという思いから `kurage` と名付けた。
